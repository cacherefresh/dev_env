


## git & ~/.zsdhrc additional steps

#### provides tabcomplete for branch names in git. 

run the following in a terminal and restart the terminal and/or vscode
```
echo 'autoload -Uz compinit && compinit' >> ~/.zshrc

source ~/.zshrc
```
